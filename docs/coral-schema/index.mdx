---
id: index
title: Coral schema
sidebar_label: Overview
---

# Coral schema

A ground-truth DOM and network audit of sixteen Coral surfaces, captured live on 2026-06-07 and
re-verified on 2026-06-17. This is the schema-mapping reference for the runtime.

## What was measured

| Measure | Count |
| --- | --- |
| Surfaces | 16 |
| Elements catalogued | 1,627 |
| Interactive elements | 1,115 |
| Elements carrying a test id | 983 |
| Unique test ids | 177 |
| Test ids shared across surfaces | 76 |
| Interactive elements with no derivable label | 228 |
| Network requests observed | 1,249 |
| Functional requests classified | 611 |
| Endpoints mapped | 57 |

Response-status breakdown of the 611 functional requests: 555 returned 200, 35 returned 404, 9
returned 500, 6 returned 405, 3 returned 412, and 3 failed outright. Full methodology for every
number is on [Audit metrics](./metrics.mdx).

## Pages in this section

| Page | Contents |
| --- | --- |
| [Naming schema](./naming.md) | Both identifier layers, area rules, state suffixes |
| [Confidence model](./confidence.md) | The three grades and the four honesty notes |
| [Surfaces](./surfaces.mdx) | All 16 surfaces with per-surface counts |
| [Test id catalog](./testid-catalog.mdx) | All 177 unique test ids |
| [Shared test ids](./shared-testids.mdx) | The 76 ids appearing on more than one surface |
| [Element explorer](./element-explorer.mdx) | All 1,627 elements, filterable |
| [Label gaps](./label-gaps.mdx) | The 228 interactive elements with no derivable label |
| [Audit metrics](./metrics.mdx) | Every headline number with its methodology |

## The two identifier layers

The audit assigns every element and every endpoint a stable semantic identifier, in two parallel
schemes.

**UI layer**

```text
coral.<area>.<surface>.<component>[.<state>]
```

**API layer**

```text
{namespace}:{surface}:{group}:{operation}
```

The API format is adopted from the authoritative Copilot Studio corpus rather than invented here, so
identifiers in this audit line up with the published model. See [Naming schema](./naming.md).

## Confidence

Every assertion carries one of three grades — `CONFIRMED`, `SOURCE-INSPECTED`, or `UNCONFIRMED`.

**This audit is `CONFIRMED` throughout.** Every element row came from a live DOM walk and every
endpoint from a live network capture.

Four honesty notes qualify every number on this page. They are load-bearing, not boilerplate, and
they are set out in full on the [confidence model](./confidence.md) page.

## Source data

All of it lives in `coral-schema/` in the source repository.

| File | Contents |
| --- | --- |
| `coral-ui-audit.html` | Self-contained interactive report. Start here. |
| `naming-schema.md` | Both identifier layers, area rules, state suffixes |
| `schema-validation.json` | Totals, per-surface counts, label gaps, shared test ids |
| `api-schema-map.json` | 11 domains, 57 endpoints, 7 hosts |
| `elements-slim.json` | 1,627 rows with abbreviated keys |
| `tab-audit-log.json` | Live designer-tab state, 2026-06-07 |
| `surfaces/*.json` | Per-surface harness output |
