{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://dayour.github.io/raybot/schemas/DesignerPitfall.schema.json",
  "$ref": "#/definitions/DesignerPitfall",
  "definitions": {
    "DesignerPitfall": {
      "type": "object",
      "properties": {
        "confidence": {
          "$ref": "#/definitions/ConfidenceLevel"
        },
        "evidence": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Capture filenames, dump filenames, or request ids that back the claim. Relative to the audit repository root."
        },
        "note": {
          "type": [
            "string",
            "null"
          ],
          "description": "Free-text qualifier, used when a claim is true only under conditions."
        },
        "id": {
          "type": "string"
        },
        "symptom": {
          "type": "string",
          "description": "Short statement of the behaviour."
        },
        "mitigation": {
          "type": "string",
          "description": "What to do instead."
        },
        "observedIn": {
          "type": "string",
          "enum": [
            "incident-triage",
            "showcase-rebuild"
          ],
          "description": "Which build surfaced it."
        }
      },
      "required": [
        "confidence",
        "id",
        "mitigation",
        "observedIn",
        "symptom"
      ],
      "description": "A designer behaviour that cost time during the showcase builds.\n\nThese are modelled as data rather than prose because several of them are preconditions for any future automation against the designer: an automation that does not close the Agent config panel before opening the next step picker will time out, every time, with no error that explains why."
    },
    "ConfidenceLevel": {
      "type": "string",
      "enum": [
        "CONFIRMED",
        "SOURCE-INSPECTED",
        "UNCONFIRMED"
      ],
      "description": "How a recorded fact was established.\n\n- `CONFIRMED` - observed directly in the running product: a DOM dump, a   captured network request, or a screenshot showing the resulting state.\n- `SOURCE-INSPECTED` - read out of shipped client source (bundle, sourcemap,   or manifest) but never exercised at runtime during the audit.\n- `UNCONFIRMED` - inferred from naming, adjacency, or documentation, with no   direct observation behind it."
    }
  }
}
