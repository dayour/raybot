{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://dayour.github.io/raybot/schemas/ApiEndpoint.schema.json",
  "$ref": "#/definitions/ApiEndpoint",
  "definitions": {
    "ApiEndpoint": {
      "type": "object",
      "properties": {
        "confidence": {
          "$ref": "#/definitions/ConfidenceLevel"
        },
        "evidence": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Capture filenames, dump filenames, or request ids that back the claim. Relative to the audit repository root."
        },
        "note": {
          "type": [
            "string",
            "null"
          ],
          "description": "Free-text qualifier, used when a claim is true only under conditions."
        },
        "domain": {
          "type": "string"
        },
        "domainTitle": {
          "type": [
            "string",
            "null"
          ],
          "description": "Human-readable domain title. Null throughout the published export: the titles live in the domain lookup, not on the endpoint row."
        },
        "method": {
          "type": "string"
        },
        "host": {
          "type": "string"
        },
        "hostName": {
          "type": "string"
        },
        "path": {
          "type": "string",
          "description": "Path with `{key}` placeholders substituted for observed identifiers."
        },
        "count": {
          "type": "number",
          "description": "Number of times the endpoint was observed during the capture window."
        },
        "purpose": {
          "type": "string"
        },
        "semanticId": {
          "type": "string"
        },
        "status": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "confidence",
        "count",
        "domain",
        "domainTitle",
        "host",
        "hostName",
        "method",
        "path",
        "purpose",
        "semanticId"
      ],
      "description": "A single observed endpoint, grouped into a functional domain."
    },
    "ConfidenceLevel": {
      "type": "string",
      "enum": [
        "CONFIRMED",
        "SOURCE-INSPECTED",
        "UNCONFIRMED"
      ],
      "description": "How a recorded fact was established.\n\n- `CONFIRMED` - observed directly in the running product: a DOM dump, a   captured network request, or a screenshot showing the resulting state.\n- `SOURCE-INSPECTED` - read out of shipped client source (bundle, sourcemap,   or manifest) but never exercised at runtime during the audit.\n- `UNCONFIRMED` - inferred from naming, adjacency, or documentation, with no   direct observation behind it."
    }
  }
}
