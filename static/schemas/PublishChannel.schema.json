{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://dayour.github.io/raybot/schemas/PublishChannel.schema.json",
  "$ref": "#/definitions/PublishChannel",
  "definitions": {
    "PublishChannel": {
      "type": "object",
      "properties": {
        "confidence": {
          "$ref": "#/definitions/ConfidenceLevel"
        },
        "evidence": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Capture filenames, dump filenames, or request ids that back the claim. Relative to the audit repository root."
        },
        "note": {
          "type": [
            "string",
            "null"
          ],
          "description": "Free-text qualifier, used when a claim is true only under conditions."
        },
        "kind": {
          "$ref": "#/definitions/PublishChannelKind"
        },
        "uiReportsEnabled": {
          "type": "boolean",
          "description": "Whether the channel toggle reports itself as enabled in the UI."
        },
        "verifiedReachable": {
          "type": "boolean",
          "description": "Whether traffic was actually observed reaching the agent."
        },
        "blockedReason": {
          "type": "string"
        }
      },
      "required": [
        "confidence",
        "kind",
        "uiReportsEnabled",
        "verifiedReachable"
      ]
    },
    "ConfidenceLevel": {
      "type": "string",
      "enum": [
        "CONFIRMED",
        "SOURCE-INSPECTED",
        "UNCONFIRMED"
      ],
      "description": "How a recorded fact was established.\n\n- `CONFIRMED` - observed directly in the running product: a DOM dump, a   captured network request, or a screenshot showing the resulting state.\n- `SOURCE-INSPECTED` - read out of shipped client source (bundle, sourcemap,   or manifest) but never exercised at runtime during the audit.\n- `UNCONFIRMED` - inferred from naming, adjacency, or documentation, with no   direct observation behind it."
    },
    "PublishChannelKind": {
      "type": "string",
      "enum": [
        "demo",
        "teams",
        "m365Copilot",
        "website",
        "customWebsite",
        "slack",
        "directLine"
      ],
      "description": "Channels observed in the Coral publish surface."
    }
  }
}
