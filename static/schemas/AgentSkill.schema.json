{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://dayour.github.io/raybot/schemas/AgentSkill.schema.json",
  "$ref": "#/definitions/AgentSkill",
  "definitions": {
    "AgentSkill": {
      "type": "object",
      "properties": {
        "confidence": {
          "$ref": "#/definitions/ConfidenceLevel"
        },
        "evidence": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Capture filenames, dump filenames, or request ids that back the claim. Relative to the audit repository root."
        },
        "note": {
          "type": [
            "string",
            "null"
          ],
          "description": "Free-text qualifier, used when a claim is true only under conditions."
        },
        "name": {
          "type": "string"
        },
        "entryMode": {
          "$ref": "#/definitions/SkillEntryMode"
        },
        "description": {
          "type": "string",
          "description": "Short summary shown on the skill card. Capped at 1024 characters."
        },
        "instructions": {
          "type": "string",
          "description": "Natural-language instructions that define the skill's behaviour."
        }
      },
      "required": [
        "confidence",
        "entryMode",
        "name"
      ]
    },
    "ConfidenceLevel": {
      "type": "string",
      "enum": [
        "CONFIRMED",
        "SOURCE-INSPECTED",
        "UNCONFIRMED"
      ],
      "description": "How a recorded fact was established.\n\n- `CONFIRMED` - observed directly in the running product: a DOM dump, a   captured network request, or a screenshot showing the resulting state.\n- `SOURCE-INSPECTED` - read out of shipped client source (bundle, sourcemap,   or manifest) but never exercised at runtime during the audit.\n- `UNCONFIRMED` - inferred from naming, adjacency, or documentation, with no   direct observation behind it."
    },
    "SkillEntryMode": {
      "type": "string",
      "enum": [
        "blank",
        "generated"
      ],
      "description": "A skill authored in the skill editor.\n\nThe editor offers two entry modes: start from a blank skill, or start from a generated draft. Raybot's `ray-cluster-troubleshooter` was authored blank."
    }
  }
}
